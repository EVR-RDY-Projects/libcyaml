---
name: branch-flow

# Per-repo stub. Delegates to the org-wide policy at:
#   EVR-RDY-Projects/.github/.github/workflows/branch-flow-reusable.yml
#
# To change the policy, edit the reusable workflow, not this file.

on:
  pull_request:
    branches:
      - main
      - develop
  merge_group:

permissions:
  contents: read

jobs:
  branch-flow:
    uses: EVR-RDY-Projects/.github/.github/workflows/branch-flow-reusable.yml@main